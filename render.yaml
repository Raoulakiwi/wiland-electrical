services:
  - type: web
    name: callpilot-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -c "from api.index import app; app.run(host='0.0.0.0', port=10000)"
    plan: free
